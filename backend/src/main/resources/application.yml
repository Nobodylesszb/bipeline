spring:
  application:
    name: pipeline-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/pipeline_dev
    username: bo
    password: 521524l
  flyway:
    enabled: true
  jpa:
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8100

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    try-it-out-enabled: true
    operations-sorter: method
    tags-sorter: alpha
